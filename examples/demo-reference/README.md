# Demo reference

The original real-world reference quote that seeded this project's layout has been
**removed for privacy** (it contained a real client's confidential pricing).

To validate your own xlsx output, create a quote in the app (`/new`) and download it.
The server-side builder lives in `src/server/quote-xlsx.ts`.
